CREATE TABLE Todo (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    isDone INTEGER NOT NULL DEFAULT 0
);

insertTodo:
INSERT INTO Todo(title, isDone)
VALUES(?, ?);

removeAllTodo:
DELETE FROM Todo;

selectAllTodo:
SELECT * FROM Todo;